import styled from "styled-components";

const LeaderBoard = () => {
    return(<div>
        <div>리더보드 상세페이지</div>
        
    </div>);
}

// input data
// player 정보

// 승점에 따른 순서 배열 

export default LeaderBoard;